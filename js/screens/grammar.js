const GrammarScreen = {
  render() {
    const el = document.getElementById("screen-grammar");
    if (!el) return;
    el.innerHTML = `<p style="padding:2rem;color:var(--text-primary);">Grammar — coming soon</p>`;
  },
};
