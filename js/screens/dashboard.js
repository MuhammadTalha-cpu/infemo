var DashboardScreen = {
  render() {
    const el = document.getElementById("screen-dashboard");
    if (!el) return;
    el.innerHTML = `<p style="padding:2rem;color:var(--text-primary);">Dashboard — coming soon</p>`;
  },
};
