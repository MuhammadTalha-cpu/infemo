const AuthScreen = {
  render() {
    const el = document.getElementById("screen-auth");
    if (!el) return;
    el.innerHTML = `<p style="padding:2rem;color:var(--text-primary);">Auth Screen — coming soon</p>`;
  },
};
