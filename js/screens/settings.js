const SettingsScreen = {
  render() {
    const el = document.getElementById("screen-settings");
    if (!el) return;
    el.innerHTML = `<p style="padding:2rem;color:var(--text-primary);">Settings — coming soon</p>`;
  },
};
